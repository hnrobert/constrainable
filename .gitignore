node_modules/
.DS_Store

app/.nuxt/
app/.output/
app/data/
app/records/
app/.env

node/records/

# protobuf generated code — NEVER committed; `bun run proto` generates
# (root: bun install once + go install protoc-gen-go once). CI generates
# before tests/builds; Docker builds pick it up from the workspace.
node/gen/
app/shared/proto/
